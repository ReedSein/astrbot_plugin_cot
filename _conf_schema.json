{
  "cot_config_group": {
    "description": "💡 思维链(CoT)核心配置",
    "type": "object",
    "items": {
      "cot_start_tag": {
        "description": "思考开始标签",
        "type": "string",
        "default": "<罗莎内心OS>",
        "hint": "模型输出中标志思考开始的标签。"
      },
      "cot_end_tag": {
        "description": "思考结束标签",
        "type": "string",
        "default": "</罗莎内心OS>",
        "hint": "模型输出中标志思考结束的标签。"
      },
      "final_reply_pattern": {
        "description": "最终回复匹配正则",
        "type": "string",
        "default": "最终的罗莎回复[:：]?\\s*",
        "hint": "用于定位思考结束后，正式回复开始的正则表达式。"
      },
      "display_cot_text": {
        "description": "显示思考过程",
        "type": "bool",
        "default": false,
        "hint": "开启后，思考过程将不会被隐藏，而是随回复一起发送（用于调试）。"
      },
      "filtered_keywords": {
        "description": "回复过滤词列表",
        "type": "list",
        "default": ["呵呵，", "（……）"],
        "hint": "这些词汇如果出现在最终回复中，会被自动剔除。"
      },
      "force_cot_structure": {
        "description": "强制CoT结构校验",
        "type": "bool",
        "hint": "开启后，若回复不包含完整的CoT标签结构，将触发重试。建议为罗莎开启。",
        "default": true
      }
    }
  },
  "retry_core_config": {
    "description": "🛡️ 重试与防御配置",
    "type": "object",
    "items": {
      "max_attempts": {
        "description": "最大重试次数",
        "type": "int",
        "hint": "底层补丁尝试重新请求的最大次数。建议 3 次。",
        "default": 3
      },
      "retry_delay": {
        "description": "重试间隔（秒）",
        "type": "int",
        "hint": "每次重试之间的固定等待时间。",
        "default": 2
      },
      "fallback_reply": {
        "description": "兜底回复",
        "type": "text",
        "hint": "当所有重试都失败后，发送给用户的友好提示。这也将作为历史记录被保存。",
        "default": "抱歉，服务波动，罗莎暂时无法回应。"
      },
      "error_keywords": {
        "description": "内容级错误关键词",
        "type": "text",
        "hint": "当回复内容（而非报错）包含这些词时，触发上层重试。每行一个。",
        "default": "api 返回的内容为空\n调用失败\n[TRUNCATED_BY_LENGTH]\nupstream error\ninternal server error\nerror generating"
      },
      "exclude_retry_commands": {
        "description": "白名单指令 (不重试)",
        "type": "text",
        "hint": "以这些指令开头的请求将跳过重试机制，直接透传。每行一个。",
        "default": "/cogito\n/rosaos\nreset\nnew"
      },
      "enable_truncation_retry": {
        "description": "启用截断重试",
        "type": "bool",
        "hint": "检测到 [TRUNCATED_BY_LENGTH] 时是否重试。",
        "default": false
      }
    }
  },
  "storage_config": {
    "description": "💾 存储与总结",
    "type": "object",
    "items": {
      "history_limit": {
        "description": "热数据保留条数",
        "type": "int",
        "default": 100
      },
      "summary_provider_id": {
        "description": "总结用小模型ID",
        "type": "string",
        "hint": "用于 /cogito 指令。留空则使用当前会话模型。",
        "default": ""
      },
      "summary_max_retries": {
        "description": "总结重试次数",
        "type": "int",
        "default": 2
      },
      "summary_timeout": {
        "description": "总结超时时间",
        "type": "int",
        "default": 60
      },
      "summary_prompt_template": {
        "description": "总结提示词模板",
        "type": "text",
        "default": "请阅读以下机器人的'内心独白(Inner Thought)'日志，用简练、客观的语言总结其核心思考逻辑、情绪状态以及最终的决策意图。\n\n日志内容：\n{log}"
      }
    }
  }
}
