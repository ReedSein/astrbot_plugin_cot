{
  "force_cot_structure": {
    "description": "强制CoT结构 (核心功能)",
    "type": "bool",
    "hint": "开启后，任何不包含<罗莎内心OS>和'最终的罗莎回复'结构的LLM响应都将被视为无效并触发重试。强烈建议为罗莎人格保持开启。",
    "default": true
  },
  "display_cot_text": {
    "description": "显示思维链文本",
    "type": "bool",
    "hint": "开启后，Bot回复中将包含'🤔 罗莎思考中：...'的思维链内容。关闭则只显示最终回复。",
    "default": false
  },
  "cot_start_tag": {
    "description": "思维链开始标签",
    "type": "string",
    "hint": "用于识别思维链开始的标签，如 <罗莎内心OS>。",
    "default": "<罗莎内心OS>"
  },
  "cot_end_tag": {
    "description": "思维链结束标签",
    "type": "string",
    "hint": "用于识别思维链结束的标签，如 </罗莎内心OS>。",
    "default": "</罗莎内心OS>"
  },
  "final_reply_pattern": {
    "description": "最终回复分隔正则",
    "type": "string",
    "hint": "用于分割思维链和最终回复的正则表达式。",
    "default": "最终的罗莎回复[:：]?\\s*"
  },
  "filtered_keywords": {
    "description": "回复关键词过滤",
    "type": "list",
    "hint": "从最终回复中剔除的关键词列表。常用于去除口癖等。",
    "default": [
      "呵呵，",
      "（……）"
    ]
  },
  "history_limit": {
    "description": "热数据保留条数",
    "type": "int",
    "hint": "在快速访问存储(JSON)中保留的最近内心OS记录数量。超出的记录仍会在每日全量归档中永久保存。",
    "default": 100
  },
  "summary_provider_id": {
    "description": "总结用小模型ID (Cogito)",
    "type": "string",
    "hint": "用于 /cogito 指令的专用模型ID（如 openai_2, ollama_1）。留空则默认使用当前会话的主模型。",
    "default": ""
  },
  "summary_max_retries": {
    "description": "总结任务重试次数",
    "type": "int",
    "hint": "小模型生成总结失败时的重试次数。建议设置为 1-2 次。",
    "default": 2
  },
  "summary_timeout": {
    "description": "总结任务超时秒数",
    "type": "int",
    "hint": "如果小模型在指定时间内未返回总结结果，将中止任务并重试或兜底。",
    "default": 60
  },
  "summary_prompt_template": {
    "description": "日志总结提示词",
    "type": "text",
    "hint": "发送给小模型用于总结日志的提示词模板。{log} 会被替换为实际日志内容。",
    "default": "请阅读以下机器人的'内心独白(Inner Thought)'日志，用简练、客观的语言总结其核心思考逻辑、情绪状态以及最终的决策意图。\n\n日志内容：\n{log}"
  },
  "enable_truncation_retry": {
    "description": "启用截断重试",
    "type": "bool",
    "hint": "开启后，检测到回复结尾不是常见标点/字母/数字/文件后缀/网址时自动重试（如 Gemini 截断），建议仅在遇到相关问题时开启。",
    "default": false
  },
  "max_attempts": {
    "description": "最大重试次数",
    "type": "int",
    "hint": "当LLM回复无效时，插件将尝试重新请求的最大次数。设置为0则禁用重试。",
    "default": 3
  },
  "retry_delay": {
    "description": "重试间隔（秒）",
    "type": "int",
    "hint": "每次重试之间的等待时间，单位为秒。",
    "default": 2
  },
  "error_keywords": {
    "description": "触发重试的错误关键词",
    "type": "text",
    "hint": "当LLM的回复或Core抛出的异常中包含这些关键词时，将触发重试。不区分大小写。",
    "default": "api 返回的内容为空\n调用失败\n[TRUNCATED_BY_LENGTH]\nupstream error\ndo_request_failed\ninternal server error\n500\n429\nrate limit\ntimeout\ntimed out\nconnection error\nmax retries"
  },
  "retryable_status_codes": {
    "description": "允许重试的HTTP状态码(每行一个)",
    "type": "text",
    "hint": "当错误文本中包含这些状态码时，允许进入重试流程。支持 400/429/502/503/504 等。",
    "default": "400\n429\n502\n503\n504"
  },
  "non_retryable_status_codes": {
    "description": "禁止重试的HTTP状态码(每行一个)",
    "type": "text",
    "hint": "当错误文本中包含这些状态码时，将直接跳过重试。留空表示不限制。",
    "default": ""
  },
  "fallback_reply": {
    "description": "重试全部失败时的兜底回复",
    "type": "text",
    "hint": "当达到最大重试次数仍失败时，发送给用户的一段友好提示。留空则不发送消息。",
    "default": "抱歉，刚才遇到服务波动，我已自动为你重试多次仍未成功。请稍后再试或换个说法。"
  },
  "exclude_retry_commands": {
    "description": "排除重试的命令列表",
    "type": "text",
    "hint": "这些命令触发的 LLM 请求不会被重试机制处理。每行一个命令，支持不同的命令前缀（如 /、&、! 等）。示例：/cogito、&cogito、/rosaos",
    "default": "/cogito\n/rosaos\nreset\nnew"
  }
}
